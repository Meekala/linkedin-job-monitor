# LinkedIn Job Monitor Configuration Template
# Copy this file to .env and fill in your actual values
# ⚠️  NEVER commit the .env file with real credentials to git!

# Discord Integration - KEEP THESE SECURE
# Create Discord webhooks for each city you want to monitor
# Go to your Discord server → Settings → Integrations → Webhooks → New Webhook
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_MAIN_WEBHOOK_ID/YOUR_MAIN_WEBHOOK_TOKEN

# City-specific Discord webhooks (optional - uses main webhook if not set)
DISCORD_WEBHOOK_URL_Remote=https://discord.com/api/webhooks/YOUR_REMOTE_WEBHOOK_ID/YOUR_REMOTE_WEBHOOK_TOKEN
DISCORD_WEBHOOK_URL_NYC=https://discord.com/api/webhooks/YOUR_NYC_WEBHOOK_ID/YOUR_NYC_WEBHOOK_TOKEN
DISCORD_WEBHOOK_URL_SF=https://discord.com/api/webhooks/YOUR_SF_WEBHOOK_ID/YOUR_SF_WEBHOOK_TOKEN
DISCORD_WEBHOOK_URL_LA=https://discord.com/api/webhooks/YOUR_LA_WEBHOOK_ID/YOUR_LA_WEBHOOK_TOKEN
DISCORD_WEBHOOK_URL_SD=https://discord.com/api/webhooks/YOUR_SD_WEBHOOK_ID/YOUR_SD_WEBHOOK_TOKEN

# Monitoring Configuration
CHECK_INTERVAL_MINUTES=30
CITIES=NYC,LA,SF,SD,Remote
JOB_TITLE="Associate Product Manager"

# Database and Logging
DATABASE_PATH=data/jobs.db
LOG_LEVEL=INFO
LOG_FILE=data/linkedin_monitor.log

# Security Notes:
# 1. Keep your webhook URLs private - they allow posting to your Discord channels
# 2. Consider creating separate webhooks for different cities/channels
# 3. Never share your .env file or commit it to version control
# 4. Regenerate webhooks if they're accidentally exposed
# 5. Use Railway's environment variables instead of .env in production